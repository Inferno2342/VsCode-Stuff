# Weather Searcher

### A simple Python project that gets real-time weather data using the OpenWeather API. It converts a city name into latitude and longitude, then retrieves the current weather conditions.

## Requirements
# requests
# python-dotenv

## Setup
# 1.Install dependencies
# pip install -r requirements.txt
